import { SecureMediaDirective } from './secure-media.directive';

describe('SecureMediaDirective', () => {
  it('should create an instance', () => {
    const directive = new SecureMediaDirective();
    expect(directive).toBeTruthy();
  });
});
